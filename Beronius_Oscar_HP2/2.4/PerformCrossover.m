function crossedGeneration = PerformCrossover(generation,pCross)
  nbrOfIndividuals = size(generation,1);
  
  for i = 1:2:nbrOfIndividuals
    firstIndividual = generation(i,:);
    secondIndividual = generation(i+1,:);
    
    
    
  end
  
end
